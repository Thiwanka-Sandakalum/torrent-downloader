/**
 * Global client-side state store.
 *
 * Placeholder — wire up Zustand or React Query as needed.
 *
 * Suggested slices:
 *   - useTaskStore  → active downloads + progress map
 *   - useMovieStore → search results cache
 *   - useAuthStore  → Auth0 token + user profile
 */

export { };
